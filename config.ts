// In a real-world application, this would be sourced from an environment variable
// (e.g., process.env.REACT_APP_API_URL) to avoid hardcoding.
// For this environment, we define it as a constant.
export const API_BASE_URL = 'http://localhost:8080';
